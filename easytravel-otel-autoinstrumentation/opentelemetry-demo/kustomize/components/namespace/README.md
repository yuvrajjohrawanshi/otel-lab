# namespace

This component adds `namespace: astroshop` to `Service`, `Deployment`, and `ServiceAccount` resources.

It exists as a workaround for a kustomize limitation: the built-in `namespace` transformer does not create the `metadata.namespace` field on resources generated by `helmCharts` that omit it from their templates. Without this component, those resources are deployed to the `default` namespace.
